# The Expo mobile app reads the API base URL from `app.json` under `extra.apiBaseUrl`.
# This file documents how to change it without touching the JSON.

# In dev (Android emulator):
npx expo start -- --clear
# The default is http://10.0.2.2:4000 (Android emulator → host machine)

# Override at runtime without rebuilding:
EXPO_PUBLIC_API_BASE_URL="https://staging.tasksphere.app" npx expo start

# In a production build, the apiBaseUrl is baked in from app.json.
# Before running `eas build`, edit app.json → `extra.apiBaseUrl` to your prod URL:
#   "apiBaseUrl": "https://api.tasksphere.app"
#   "apiBaseUrlWeb": "https://api.tasksphere.app"
# Then run:
#   eas build --platform android --profile production
#   eas build --platform ios --profile production
